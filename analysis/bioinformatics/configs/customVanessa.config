// Cluster/time overrides and reproducibility pins
process {
  withName: 'NFCORE_RNASEQ:RNASEQ:ALIGN_STAR:STAR_ALIGN' {
    time = 100.h
  }
}

params {
  // Example: pin a specific nf-core/rnaseq version via container/tag if desired
  // genomes { }
}

// Add any UPPMAX-specific settings here (queue, scratch, etc.) if needed


